#include "AST.hpp"

optional<Value*> ClassExpr::codegen(CompilationContext &ctx) {
    // get types of all vars 
    

    // create struct
    // StructType* st = StructType::create(*ctx.context, types);
    
     
}